namespace DirectPin
{
    public enum DpTypeTransaction { NONE, DEBIT, CREDIT, VOUCHER, PIX }
    public enum DpCreditType { NO_INSTALLMENT, INSTALLMENT }
    public enum DpInterestType { MERCHANT, ISSUER }
}
